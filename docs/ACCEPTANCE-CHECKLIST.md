# Приёмка на кассе

Проверку выполняйте в спокойное время вместе с ответственным сотрудником. Перед началом сохраните обычный X-отчёт/контрольные суммы штатным способом.

## Установка

Проверка версии 0.2.12:

- [ ] Иконка видна в EXE, трее, ярлыке меню «Пуск» и установщике.
- [ ] «Обновить суммы» запрашивает отчёт; «Проверить обновление программы…» проверяет релиз без запроса к терминалу.
- [ ] При недоступном журнале, открытых настройках/диагностике или запросе отчёта автоустановка не начинается.
- [ ] Выход новой сборки во время чтения большого журнала не зависает; после запуска последняя сверка сохранена, новые операции не пропущены и не учтены дважды.
- [ ] Повторная установка поверх новой работающей сборки с закрытыми настройками и скрытым оверлеем проходит без задержки 25–30 секунд; проверка с `/NOFORCECLOSEAPPLICATIONS` тоже завершается успешно. Первый переход со старой сборки может потребовать выхода через трей.
- [ ] Диагностика объясняет состояние Frontol и журнала, копируется без банковских данных; при переходе к трею последняя причина Frontol остаётся доступной.
- [ ] Без сохранённой ширины используется 370; ранее настроенная ширина не меняется. Сброс данных не сбрасывает оформление и не удаляет банковские файлы.

- [ ] Установщик 0.2.12 запущен поверх предыдущей установленной версии в спокойное время; перед установкой монитор закрыт через трей.
- [ ] После обновления сохранились настройки, суммы и автозапуск; приложение снова запустилось.
- [ ] Выбрана существующая папка UPOS/SC552, а не её копия внутри приложения.
- [ ] В папке видны pilot_nt.dll, pinpad.ini и актуальный sbkernelYYMM.log.
- [ ] В дистрибутиве нет банковских DLL и журналов клиента.
- [ ] Приложение запускается без прав администратора.

## Оверлей

- [ ] При активном Frontol примерно за секунду появляются название и сумма каждой организации без отдельной стрелки; справа виден край ⋮ для изменения ширины.
- [ ] Названия и актуальные суммы используют выбранный цвет текста (по умолчанию белый), без фона, тени и обводки; устаревшие данные, ожидание и ошибка отмечены цветом предупреждений (по умолчанию янтарным).
- [ ] При одном отделе видна одна строка; при двух — две.
- [ ] Нет общего итога и номера отдела.
- [ ] Оверлей не закрывает важные элементы Frontol.
- [ ] Его можно передвинуть за название; после перезапуска позиция сохраняется.
- [ ] Перетягивание правого края меняет только ширину; шрифт не меняется, ширина сохраняется после перезапуска.
- [ ] При открытии настроек оверлей виден даже без Frontol; его можно передвинуть и изменить ширину мышью, поле ширины синхронизируется после отпускания края.
- [ ] Размер шрифта, выбор Segoe UI / Arial / Tahoma и отдельная жирность названий и сумм меняются сразу; полные суммы и край изменения ширины остаются читаемыми.
- [ ] Буквы сглажены на белом и тёмном фоне, цветной каймы нет. Прозрачные промежутки пропускают нажатия к Frontol, двойной щелчок по сумме и перетягивание названия/края работают.
- [ ] Цвет обычного текста и предупреждений выбирается в настройках и сразу виден на оверлее; проверена читаемость на белом и тёмном фоне Frontol.
- [ ] Выбор цвета предупреждений временно подсвечивает суммы, не меняя числа; после закрытия настроек цвет снова соответствует состоянию данных.
- [ ] «Отмена» и крестик возвращают прежние ширину, шрифт, цвета и положение; «Сохранить» запоминает их, в том числе после перезапуска.
- [ ] После «Сохранить», «Отмена» и закрытия настроек крестиком оверлей появляется примерно за секунду при возврате в главное окно Frontol; проверено на кассе, где 0.2.8 его теряла.
- [ ] При визуальном поиске, другом активном диалоге или окне загрузки оверлей скрывается; после возврата в главное окно снова появляется на прежнем месте. Проверены оба используемых интерфейса касс.
- [ ] При переходе в другую программу или сворачивании Frontol оверлей скрывается, если настройки закрыты.
- [ ] В покое CPU монитора не держится на прежних 5–6%; проверено несколько минут при активном Frontol и при скрытом оверлее.
- [ ] Прозрачная область не мешает нажимать элементы Frontol.

- [ ] При масштабе Windows 100%, 150% и 200% суммы с копейками и ₽ помещаются целиком и не перекрывают край изменения ширины.

## Автоматический расчёт

- [ ] До первой операции сумма совпадает с текущим состоянием смены либо показана как неизвестная.
- [ ] Успешная оплата отдела 1 увеличивает только первую организацию.
- [ ] Успешная оплата отдела 2 увеличивает только вторую организацию.
- [ ] Отказ банка не меняет сумму.
- [ ] Успешный возврат уменьшает нужную организацию.
- [ ] Обновление появляется примерно в течение одной секунды после финального ответа банка.
- [ ] Перезапуск приложения сохраняет правильные суммы.
- [ ] После одной успешной банковской сверки обе суммы становятся 0,00 ₽, в том числе у организации без операций.
- [ ] С отключённой галочкой второго эквайринга обнуление работает; повторное пустое закрытие при включённой галочке не меняет нули.
- [ ] Неуспешная сверка не обнуляет суммы.
- [ ] После обновления со старой контрольной точки до сверки учитываются только операции новой смены; повторный перезапуск не дублирует их.
- [ ] Пустые названия позже заполняются автоматически, а введённая вручную подпись не меняется.

## Ручной _get_statistics — обязательная контролируемая проверка

- [ ] В терминале нет активной оплаты/возврата.
- [ ] Зафиксировано, что банковская смена открыта.
- [ ] Выполнен двойной щелчок левой кнопкой по любой сумме; обновляются все организации. Одиночный и правый щелчок запрос не запускают.
- [ ] Во время запроса над суммой виден курсор ожидания, подсказка объясняет состояние; повторный двойной щелчок не запускает второй запрос. Отложенный запрос отмечен цветом предупреждений, всплывающих сообщений нет.
- [ ] Отложенный запрос запускается после завершения операции без повторного нажатия.
- [ ] Терминал вернул текущий отчёт без печати через ККТ.
- [ ] После запроса банковская смена осталась открыта.
- [ ] В sbkernel запросу соответствует Command 7000, а не 6000. Заголовок и результат close_day для 7000 нормальны и не означают закрытия смены.
- [ ] После 7000 суммы не обнуляются, данные не становятся устаревшими, следующий запрос не блокируется.
- [ ] После успешного или неудачного запроса нет всплывающего сообщения. При ошибке причина остаётся в подсказке каждой суммы и журнале, суммы используют цвет предупреждений и не изменяются.
- [ ] Название первой секции отчёта соответствует Department 1.
- [ ] Название второй секции отчёта соответствует Department 2.
- [ ] Суммы оверлея совпадают с банковским отчётом.
- [ ] При двух организациях обе суммы изменились одновременно.
- [ ] После ручной сверки и перезапуска сумма сохраняется, а следующая успешная оплата добавляется ровно один раз.

Если формат не распознан, не меняйте парсер по фотографии с карточными реквизитами. Скопируйте отчёт, удалите PAN, RRN, Terminal ID, Merchant ID, коды авторизации и номера чеков, затем сохраните обезличенный пример для теста.

## Отказоустойчивость

- [ ] При временно недоступном журнале последняя сумма получает цвет предупреждений, но не обнуляется; причина доступна в подсказке.
- [ ] После выбора другой папки UPOS суммы прежнего терминала не показываются.
- [ ] При отключённом терминале ручной запрос показывает понятную ошибку и сохраняет прежние суммы.
- [ ] Двойной щелчок по сумме во время операции откладывается до безопасного момента.
- [ ] Двойной щелчок по сумме во время незавершённой сверки откладывается до её завершения, но не ждёт повторной пустой сверки.
- [ ] Автозапуск можно включить и выключить в настройках.
- [ ] В diagnostics.log нет текста банковского отчёта и карточных реквизитов.

## Обновление

- [ ] Portable-копия не выполняет автоматическое обновление.
- [ ] После установки 0.2.12 сохраняются настройки и автозапуск, суммы восстановлены по журналу; при переходе со старой версии без цветов они остаются белым и янтарным, шрифт — Segoe UI с выделенными суммами.
- [ ] Касса с устаревшими суммами в 0.2.11 обновлена вручную поверх существующей версии: старая версия в этом состоянии не запускает автоустановку. Журналы и настройки не удалялись.
- [ ] При наличии следующего релиза проверка выполняется без перезапуска программы через 6 часов.
- [ ] Загрузка не прерывает кассовую работу; установка ждёт 30 секунд без операций, запросов и открытых окон приложения.
- [ ] Старый монитор выходит без диспетчера задач; установщик запускает новую копию, не закрывая Frontol и не перезагружая Windows.
- [ ] При автообновлении, если старый процесс не завершился за 30 секунд, установка отменяется с записью в updates/installer.log.
- [ ] Более новая публикация скачивается только под точным ожидаемым именем и запускается после совпадения SHA-256.
- [ ] Повреждённый установщик или манифест не запускается, текущая версия продолжает работу.
